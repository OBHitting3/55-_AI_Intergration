# Inbox (manual triage)

Unclassified files from `drive-import/` land here after inventory runs.

Copy loose notes or single files from your external drive into `drive-import/`, run `npm run drive:inventory`, then move unrecognized items into dated subfolders under this inbox.
